'use strict';

module.exports = appInfo => {
  const config = {};

  // should change to your own
  config.keys = appInfo.name + '_1499936322593_4922';

  // add your config here

  return config;
};
